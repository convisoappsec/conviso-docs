---
id: contact
title: contact
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Return contact from a contract

```graphql
contact(
  id: ID!
): Contact

```

### Arguments

#### `id` ([`ID!`](/api/graphql/documentation/scalars/id))

### Type

#### [`Contact`](/api/graphql/documentation/objects/contact)

---
id: custom-field-integration
title: customFieldIntegration
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all custom fields by integration id

```graphql
customFieldIntegration(
  id: Int!
): CustomFieldIntegrationCollection

```

### Arguments

#### `id` ([`Int!`](/api/graphql/documentation/scalars/int))

### Type

#### [`CustomFieldIntegrationCollection`](/api/graphql/documentation/objects/custom-field-integration-collection)

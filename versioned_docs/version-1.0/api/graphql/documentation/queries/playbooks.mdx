---
id: playbooks
title: playbooks
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Return all playbooks the company

```graphql
playbooks(
  id: ID!
  page: Int = 0
  limit: Int = 0
  params: RequirementSearch
): RequirementCollection

```

### Arguments

#### `id` ([`ID!`](/api/graphql/documentation/scalars/id))

#### `page` ([`Int`](/api/graphql/documentation/scalars/int))

#### `limit` ([`Int`](/api/graphql/documentation/scalars/int))

#### `params` ([`RequirementSearch`](/api/graphql/documentation/inputs/requirement-search))

### Type

#### [`RequirementCollection`](/api/graphql/documentation/objects/requirement-collection)

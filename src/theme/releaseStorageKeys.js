// Shared sessionStorage keys for the Release Notes UX (filters + last-doc).
// Kept in a neutral module so both Root.js and BlogListPage can import it
// without depending on Docusaurus theme-alias resolution.
export const LAST_DOC_KEY = 'conviso-last-doc-path';
export const FILTERS_STORAGE_KEY = 'conviso-release-filters';

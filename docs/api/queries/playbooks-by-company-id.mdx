---
id: playbooks-by-company-id
title: playbooksByCompanyId
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Return all playbooks the company

```graphql
playbooksByCompanyId(
  id: ID!
  page: Int = 0
  limit: Int = 0
  params: PlaybookSearch
): PlaybookCollection

```

### Arguments

#### `id` ([`ID!`](/api/scalars/id))

#### `page` ([`Int`](/api/scalars/int))

#### `limit` ([`Int`](/api/scalars/int))

#### `params` ([`PlaybookSearch`](/api/inputs/playbook-search))

### Type

#### [`PlaybookCollection`](/api/objects/playbook-collection)

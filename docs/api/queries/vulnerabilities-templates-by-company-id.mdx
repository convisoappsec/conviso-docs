---
id: vulnerabilities-templates-by-company-id
title: vulnerabilitiesTemplatesByCompanyId
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all vulnerabilities templates by company id

```graphql
vulnerabilitiesTemplatesByCompanyId(
  id: ID!
  page: Int = 0
  limit: Int = 0
): VulnerabilityTemplateCollection!

```

### Arguments

#### `id` ([`ID!`](/api/scalars/id))

#### `page` ([`Int`](/api/scalars/int))

#### `limit` ([`Int`](/api/scalars/int))

### Type

#### [`VulnerabilityTemplateCollection`](/api/objects/vulnerability-template-collection)

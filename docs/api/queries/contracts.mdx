---
id: contracts
title: contracts
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all contracts

```graphql
contracts(
  page: Int = 0
  limit: Int = 0
  sortBy: String
  descending: Boolean
  params: ContractSearch
): ContractCollection!

```

### Arguments

#### `page` ([`Int`](/api/scalars/int))

#### `limit` ([`Int`](/api/scalars/int))

#### `sortBy` ([`String`](/api/scalars/string))

#### `descending` ([`Boolean`](/api/scalars/boolean))

#### `params` ([`ContractSearch`](/api/inputs/contract-search))

### Type

#### [`ContractCollection`](/api/objects/contract-collection)

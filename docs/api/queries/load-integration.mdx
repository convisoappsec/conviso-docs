---
id: load-integration
title: loadIntegration
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Load company integration

```graphql
loadIntegration(
  companyId: Int!
  integrationType: String!
): ScopeIntegration

```

### Arguments

#### `companyId` ([`Int!`](/api/scalars/int))

#### `integrationType` ([`String!`](/api/scalars/string))

### Type

#### [`ScopeIntegration`](/api/objects/scope-integration)

---
id: analysis-types
title: analysisTypes
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all project types

```graphql
analysisTypes(
  page: Int = 0
  limit: Int = 0
  params: ProjectTypeSearch
): AnalysisTypeCollection!

```

### Arguments

#### `page` ([`Int`](/api/scalars/int))

#### `limit` ([`Int`](/api/scalars/int))

#### `params` ([`ProjectTypeSearch`](/api/inputs/project-type-search))

### Type

#### [`AnalysisTypeCollection`](/api/objects/analysis-type-collection)

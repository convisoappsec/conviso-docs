---
id: portal-user-profile
title: portalUserProfile
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Return user profile by email

```graphql
portalUserProfile(
  email: String!
): PortalUser

```

### Arguments

#### `email` ([`String!`](/api/scalars/string))

### Type

#### [`PortalUser`](/api/objects/portal-user)

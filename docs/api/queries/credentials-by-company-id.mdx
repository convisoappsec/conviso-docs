---
id: credentials-by-company-id
title: credentialsByCompanyId
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all credentials by company id

```graphql
credentialsByCompanyId(
  id: ID!
  page: Int = 0
  limit: Int = 0
): CredentialCollection!

```

### Arguments

#### `id` ([`ID!`](/api/scalars/id))

#### `page` ([`Int`](/api/scalars/int))

#### `limit` ([`Int`](/api/scalars/int))

### Type

#### [`CredentialCollection`](/api/objects/credential-collection)

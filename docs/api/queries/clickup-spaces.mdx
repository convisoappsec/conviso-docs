---
id: clickup-spaces
title: clickupSpaces
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Load Clickup spaces

```graphql
clickupSpaces(
  companyId: Int!
): [ClickupSpace!]

```

### Arguments

#### `companyId` ([`Int!`](/api/scalars/int))

### Type

#### [`ClickupSpace`](/api/objects/clickup-space)

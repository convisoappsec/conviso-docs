---
id: allocated-analyses
title: allocatedAnalyses
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all analyses that the analyst is allocated

```graphql
allocatedAnalyses(
  page: Int = 0
  limit: Int = 0
): AnalysisCollection!

```

### Arguments

#### `page` ([`Int`](/api/scalars/int))

#### `limit` ([`Int`](/api/scalars/int))

### Type

#### [`AnalysisCollection`](/api/objects/analysis-collection)

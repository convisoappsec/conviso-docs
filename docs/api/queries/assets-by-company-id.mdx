---
id: assets-by-company-id
title: assetsByCompanyId
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all assets the user has access

```graphql
assetsByCompanyId(
  id: ID!
  page: Int = 0
  limit: Int = 0
): AssetCollection!

```

### Arguments

#### `id` ([`ID!`](/api/scalars/id))

#### `page` ([`Int`](/api/scalars/int))

#### `limit` ([`Int`](/api/scalars/int))

### Type

#### [`AssetCollection`](/api/objects/asset-collection)

---
id: service-type
title: serviceType
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Return the service type

```graphql
serviceType(
  id: ID!
): ServiceType

```

### Arguments

#### `id` ([`ID!`](/api/graphql/documentation/scalars/id))

### Type

#### [`ServiceType`](/api/graphql/documentation/objects/service-type)

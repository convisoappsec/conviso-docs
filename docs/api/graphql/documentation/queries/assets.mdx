---
id: assets
title: assets
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Returns all assets the user has access

```graphql
assets(
  id: ID!
  page: Int = 0
  limit: Int = 0
): AssetCollection!

```

### Arguments

#### `id` ([`ID!`](/api/graphql/documentation/scalars/id))

#### `page` ([`Int`](/api/graphql/documentation/scalars/int))

#### `limit` ([`Int`](/api/graphql/documentation/scalars/int))

### Type

#### [`AssetCollection`](/api/graphql/documentation/objects/asset-collection)

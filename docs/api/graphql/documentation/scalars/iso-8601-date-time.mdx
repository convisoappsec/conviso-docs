---
id: iso-8601-date-time
title: ISO8601DateTime
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An ISO 8601-encoded datetime

```graphql
scalar ISO8601DateTime
```

---
id: order-params
title: OrderParams
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum OrderParams {
  DESC
  ASC
}
```

### Values

#### `DESC`

descending order

#### `ASC`

ascending order

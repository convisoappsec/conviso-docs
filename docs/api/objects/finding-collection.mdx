---
id: finding-collection
title: FindingCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type FindingCollection {
  collection: [Finding!]!
  metadata: CollectionMetadata!
}
```

### Fields

#### `collection` ([`[Finding!]!`](/api/objects/finding))

#### `metadata` ([`CollectionMetadata!`](/api/objects/collection-metadata))

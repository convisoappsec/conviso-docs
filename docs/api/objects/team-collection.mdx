---
id: team-collection
title: TeamCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type TeamCollection {
  collection: [Team!]!
  metadata: CollectionMetadata!
}
```

### Fields

#### `collection` ([`[Team!]!`](/api/objects/team))

#### `metadata` ([`CollectionMetadata!`](/api/objects/collection-metadata))

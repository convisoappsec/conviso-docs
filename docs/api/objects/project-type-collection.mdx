---
id: project-type-collection
title: ProjectTypeCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ProjectTypeCollection {
  collection: [ProjectType!]!
  metadata: CollectionMetadata!
}
```

### Fields

#### `collection` ([`[ProjectType!]!`](/api/objects/project-type))

#### `metadata` ([`CollectionMetadata!`](/api/objects/collection-metadata))

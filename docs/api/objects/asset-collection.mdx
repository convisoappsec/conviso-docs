---
id: asset-collection
title: AssetCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AssetCollection {
  collection: [Asset!]!
  metadata: CollectionMetadata!
}
```

### Fields

#### `collection` ([`[Asset!]!`](/api/objects/asset))

#### `metadata` ([`CollectionMetadata!`](/api/objects/collection-metadata))

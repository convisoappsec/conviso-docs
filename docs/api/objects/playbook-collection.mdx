---
id: playbook-collection
title: PlaybookCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PlaybookCollection {
  collection: [Playbook!]!
  metadata: CollectionMetadata!
}
```

### Fields

#### `collection` ([`[Playbook!]!`](/api/objects/playbook))

#### `metadata` ([`CollectionMetadata!`](/api/objects/collection-metadata))

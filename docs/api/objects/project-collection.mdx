---
id: project-collection
title: ProjectCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ProjectCollection {
  collection: [Project!]!
  metadata: CollectionMetadata!
}
```

### Fields

#### `collection` ([`[Project!]!`](/api/objects/project))

#### `metadata` ([`CollectionMetadata!`](/api/objects/collection-metadata))

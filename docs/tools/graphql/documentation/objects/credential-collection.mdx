---
id: credential-collection
title: CredentialCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CredentialCollection {
  collection: [Credential!]!
  metadata: CollectionMetadata!
}
```

### Fields

#### `collection` ([`[Credential!]!`](/api/graphql/documentation/objects/credential))

#### `metadata` ([`CollectionMetadata!`](/api/graphql/documentation/objects/collection-metadata))

---
id: contract
title: contract
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Return the contract the user has access

```graphql
contract(
  id: ID!
): Contract

```

### Arguments

#### `id` ([`ID!`](/api/graphql/documentation/scalars/id))

### Type

#### [`Contract`](/api/graphql/documentation/objects/contract)

---
id: project
title: project
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Return the project if the user has access

```graphql
project(
  id: ID!
): Project

```

### Arguments

#### `id` ([`ID!`](/api/graphql/documentation/scalars/id))

### Type

#### [`Project`](/api/graphql/documentation/objects/project)

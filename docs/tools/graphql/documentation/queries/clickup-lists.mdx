---
id: clickup-lists
title: clickupLists
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Load Clickup lists

```graphql
clickupLists(
  companyId: Int!
  folderId: Int!
): [ClickupList!]

```

### Arguments

#### `companyId` ([`Int!`](/api/graphql/documentation/scalars/int))

#### `folderId` ([`Int!`](/api/graphql/documentation/scalars/int))

### Type

#### [`ClickupList`](/api/graphql/documentation/objects/clickup-list)

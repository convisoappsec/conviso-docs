---
id: asset-severity
title: AssetSeverity
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum AssetSeverity {
  high
  critical
  low
  none
}
```

### Values

#### `high`

High

#### `critical`

Critical

#### `low`

Low

#### `none`

None
